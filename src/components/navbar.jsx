import { NavLink } from "react-router-dom";
import { ROUTERS } from "../constants/routers";

export default function Navbar() {
  const baseBtn = "tw-btn tw-btn-ghost";
  const activeBtn = "tw-btn tw-btn-primary";
  return <button></button>;
}
